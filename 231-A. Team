#include<stdio.h>
int main()

{
    int x,i,a,b,c,count=0;
    scanf("%d",&x);
    for(i=1;i<=x;i++)
    {
        scanf("%d %d %d",&a,&b,&c);
        if(a+b+c>=2)
            count++;
    }
    printf("%d\n",count);
}
