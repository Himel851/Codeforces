#include<stdio.h>
#include<string.h>
int main()
{
   char a[200];
   int i,l;
   gets(a);
   l=strlen(a);
   for(i=0;i<l;i++)
   {
       if(a[i]=='H' || a[i]=='Q' || a[i]=='9')
       {
           printf("YES");
           return 0;
       }
   }
    printf("NO");
 
    return 0;
}
