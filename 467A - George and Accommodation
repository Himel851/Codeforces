#include<iostream>
using namespace std;
int main()

{
    int a,x,y,count=0;
    cin>>a;
    for(int i=1;i<=a;i++)
    {
        cin>>x>>y;
        if(x<=(y-2))
            count++;
    }
    cout<<count<<endl;
}
