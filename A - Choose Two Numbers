#include<iostream>
#include<algorithm>
using namespace std;

int main()
{
    int x,y,a[500],b[500];
    cin>>x;
    for(int i=0;i<x;i++)
        cin>>a[i];

    sort(a,a+x);

    cin>>y;
    for(int i=0;i<y;i++)
        cin>>b[i];

    sort(b,b+y);

    cout<<a[x-1]<<' '<<b[y-1]<<endl;


}
