#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a[10];
    cin>>a[0]>>a[1];
    sort(a,a+2);
    if(a[0]%2==0)
        cout<<"Malvika"<<endl;
    else
        cout<<"Akshat"<<endl;
}
