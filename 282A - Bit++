#include<stdio.h>
int main()

{
    int x,i,count=0;
    char a[5];

    scanf("%d",&x);
    for(i=1;i<=x;i++)
    {
        scanf("%s",&a);
        if(a[1]=='-')
        {
            count--;
        }
        else
            count++;

    }
    printf("%d\n",count);

    return 0;
}
